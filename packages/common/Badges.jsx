import "./style.css";

export const UnknownUserBadge = () => (
  <header className="user-badge unknown-user-badge" />
);

export const AuthenticatedUserBadge = () => (
  <header className="user-badge authenticated-user-badge" />
);
