import "../style.css";

export const AuthButton = ({ children }) => (
  <button className="button auth-button">{children}</button>
);

export const ApproveButton = ({ children }) => (
  <button className="button approve-button">{children}</button>
);
